export function Nav() {
  return (
    <nav
      style={{
        height: '54px',
        position: 'fixed',
        top: 0,
        left: 0,
        right: 0,
        background: 'var(--background)',
        borderBottom: '1px solid var(--border)',
        zIndex: 50,
      }}
    />
  );
}
